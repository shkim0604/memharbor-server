services:
  memharbor_server:
    build:
      context: .
      dockerfile: Dockerfile
    command: gunicorn --chdir /app config.wsgi:application --bind 0.0.0.0:8001
    env_file:
      - .env.dev
    environment:
      - RECORDER_SERVICE_URL=http://recorder:3100
      - TZ=America/New_York
    expose:
      - "8001"
    volumes:
      - ./logs:/app/logs
      - ./firebase:/app/firebase:ro
    depends_on:
      - recorder
    restart: unless-stopped

  recorder:
    build:
      context: ./recorder
      dockerfile: Dockerfile
    env_file:
      - .env.dev
    environment:
      - RECORDER_PORT=3100
      - RECORDINGS_DIR=/app/recordings
      - TZ=America/New_York
    expose:
      - "3100"
    volumes:
      - ./recordings:/app/recordings
      - ./logs:/app/logs
      - ./firebase:/app/firebase:ro
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    depends_on:
      - memharbor_server
    ports:
      - "8001:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped

  cloudflared:
    image: cloudflare/cloudflared:latest
    command: tunnel run
    environment:
      - TUNNEL_TOKEN=eyJhIjoiYTY5MDU0OTAxNDNiYTdmNDU4MzQxNWIzMjYzMmM1NjQiLCJ0IjoiY2IwNWZiMDgtYzFkNy00M2Q5LWFmMjktZWM3ZGYzOGJiNGUwIiwicyI6Ik56RXlZelkwTTJVdE5HUmhZeTAwTkRFMExXSmtOV1F0WmpCbE5HVTVZbVV4TXpsayJ9
    restart: unless-stopped

# App data is stored in Firebase Firestore (no local DB needed)
